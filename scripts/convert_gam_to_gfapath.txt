vg view -a alns.gam | jq '"P\t" + .name + "\t" + ([.path.mapping[].position | ((.node_id | tostring) + (if .is_reverse != null then "-" else "+" end))] | join(",")) + "\t*"' | tr -d '"' | sed 's/\\t/\t/g'